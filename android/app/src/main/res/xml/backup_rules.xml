<?xml version="1.0" encoding="utf-8"?>
<!--
   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API Level 23 and
   instead uses the legacy full backup behavior.
-->
<full-backup-content>
    <!-- Exclude specific shared preferences that contain GCM registration Id -->
    <exclude domain="sharedpref" path="flutter.preferences" />
    <exclude domain="sharedpref" path="jpush_preferences" />
    <exclude domain="sharedpref" path="umeng_preferences" />
    <!-- TODO: Use <include> and <exclude> to control what is backed up.
    <include domain="file" path="dd"/>
    <exclude domain="file" path="dd/fo3o.txt"/>
    <exclude domain="file" path="dd/ss/foo.txt"/>
    -->
</full-backup-content>
