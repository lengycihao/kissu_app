# 位置提醒功能测试清单

## ✅ 已完成的功能

### 1. 高德地图Web API配置
- [x] 更新Web服务API Key: `347b46716d628b9464546b31726ba3fc`
- [x] 逆地理编码服务已配置完成

### 2. 地图Marker功能
- [x] 点击地图显示marker
- [x] Marker显示逆地理编码的地址（通过InfoWindow）
- [x] 地址加载状态显示（"正在获取地址..."）
- [x] 地址获取成功后自动更新marker

### 3. 初始位置支持
- [x] 支持传入初始经纬度
- [x] 支持传入初始位置名称
- [x] 地图相机自动定位到初始位置
- [x] 初始位置自动显示marker和地址

### 4. UI优化
- [x] 底部面板显示当前选中位置的地址卡片
- [x] 地址卡片带位置图标
- [x] 加载状态视觉反馈
- [x] 地址过长自动截断（最多2行）
- [x] 更新使用说明文字

### 5. 集成当前定位
- [x] "添加地点"按钮点击时获取当前位置
- [x] 当前位置作为初始位置传递给地图选点页面
- [x] 无当前位置时使用默认位置（杭州）

## 🧪 测试步骤

### 基础功能测试
1. **打开位置提醒页面**
   ```
   导航路径: [根据您的应用导航到位置提醒页面]
   预期: 页面正常打开，显示已保存的位置提醒列表和"添加地点"按钮
   ```

2. **点击"添加地点"**
   ```
   操作: 点击"添加地点"按钮
   预期: 
   - 地图选点页面打开
   - 如果有定位权限，地图自动定位到当前位置
   - 当前位置显示红色marker
   - Marker上显示"正在获取地址..."
   ```

3. **验证地址获取**
   ```
   预期:
   - 几秒后，marker的InfoWindow更新为实际地址
   - 底部面板的地址卡片显示完整地址
   - 地址格式类似: "浙江省杭州市上城区..."
   ```

4. **点击地图其他位置**
   ```
   操作: 点击地图上的不同位置
   预期:
   - Marker移动到新位置
   - 立即显示"正在获取地址..."
   - 几秒后显示新位置的实际地址
   - 底部面板地址同步更新
   ```

5. **点击Marker**
   ```
   操作: 点击地图上的marker
   预期: InfoWindow弹出，显示详细地址信息
   ```

6. **选择图标和填写备注**
   ```
   操作:
   - 选择位置类型图标（家/公司/健身房/娱乐/商店）
   - 填写备注文字（最多10个字符）
   - 选择提醒类型（到达/离开）
   预期: 所有选项都能正常选择和输入
   ```

7. **保存位置提醒**
   ```
   操作: 点击"完成"按钮
   预期:
   - 返回位置提醒列表页面
   - 新添加的位置显示在列表中
   - 显示地图快照、地址、图标等信息
   ```

### 边界情况测试

1. **无网络连接**
   ```
   操作: 关闭网络后点击地图
   预期: 
   - Marker正常显示
   - 地址显示为"无法获取地址信息"或"地址获取失败"
   - 不影响其他功能使用
   ```

2. **GPS未授权**
   ```
   操作: 拒绝位置权限后打开地图
   预期:
   - 地图定位到默认位置（杭州）
   - 不显示初始marker（因为没有当前位置）
   - 用户可以手动点击地图选择位置
   ```

3. **地址特别长**
   ```
   操作: 选择一个地址名称特别长的位置
   预期:
   - 底部面板地址显示最多2行，超出部分用...截断
   - InfoWindow完整显示地址（可能会换行）
   ```

4. **快速点击多个位置**
   ```
   操作: 快速连续点击地图多个位置
   预期:
   - Marker位置快速更新
   - 地址获取可能有延迟，但最终显示最后一次点击的位置地址
   - 不会出现地址和marker位置不匹配的情况
   ```

### 性能测试

1. **页面加载速度**
   ```
   测试: 多次打开和关闭地图选点页面
   预期: 页面加载流畅，无明显卡顿
   ```

2. **地图交互流畅度**
   ```
   测试: 缩放、拖动、旋转地图
   预期: 所有操作流畅，无卡顿
   ```

3. **地址获取响应时间**
   ```
   测试: 记录从点击地图到地址显示的时间
   预期: 通常在1-3秒内完成
   ```

## 🐛 可能的问题和解决方案

### 问题1: InfoWindow不显示
**症状**: 点击marker后没有弹出信息窗口
**可能原因**: 
- `infoWindowEnable` 未设置为true
- 地图版本不兼容
**解决方案**: 检查Marker配置，确保启用了InfoWindow

### 问题2: 地址一直显示"正在获取地址..."
**症状**: 地址永远是加载状态
**可能原因**:
- API Key无效或配额用尽
- 网络请求失败
- 坐标超出中国范围（高德地图仅支持中国）
**解决方案**: 
- 检查API Key是否正确
- 查看日志输出的错误信息
- 确认网络连接正常

### 问题3: 地址显示不完整
**症状**: 地址信息缺失或格式不正确
**可能原因**:
- 逆地理编码返回的数据格式变化
- 某些位置的地址信息不完整
**解决方案**: 查看API返回的原始数据，调整解析逻辑

### 问题4: 当前位置不准确
**症状**: 初始位置偏离实际位置
**可能原因**:
- GPS信号弱
- 定位服务未启动
**解决方案**: 
- 检查SimpleLocationService是否正常工作
- 确认定位权限已授予

## 📝 代码验证清单

- [x] 所有文件无编译错误
- [x] 逆地理编码服务API Key已更新
- [x] Marker配置包含InfoWindow
- [x] 初始位置参数正确传递
- [x] 底部面板UI正确显示地址
- [x] 错误处理逻辑完整

## 🎯 关键代码验证点

### 1. API Key配置
```dart
// lib/services/amap_geocode_service.dart
static const String _webApiKey = '347b46716d628b9464546b31726ba3fc';
```

### 2. Marker配置
```dart
// lib/pages/location/location_reminder/location_picker/location_picker_controller.dart
final marker = Marker(
  position: position,
  icon: BitmapDescriptor.defaultMarkerWithHue(BitmapDescriptor.hueRed),
  infoWindowEnable: true, // ✓ 必须为true
  infoWindow: InfoWindow(
    title: '选中位置',
    snippet: address ?? '未知地址',
  ),
);
```

### 3. 初始位置传递
```dart
// lib/pages/location/location_reminder/location_reminder_page.dart
LocationPickerPage(
  initialLatitude: currentLoc != null 
      ? double.tryParse(currentLoc.latitude) 
      : null,
  initialLongitude: currentLoc != null 
      ? double.tryParse(currentLoc.longitude) 
      : null,
  initialLocationName: currentLoc?.locationName,
)
```

## ✅ 验收标准

功能完成的标准：
1. ✓ 点击地图能显示marker
2. ✓ Marker上通过InfoWindow显示逆地理编码的地址
3. ✓ 底部面板实时显示选中位置地址
4. ✓ 打开页面时自动显示当前位置
5. ✓ 地址获取失败时有合理的提示
6. ✓ 所有UI元素显示正常
7. ✓ 无编译错误和警告

## 📸 测试截图建议

建议截取以下场景的截图：
1. 地图选点页面初始状态（显示当前位置marker）
2. 点击地图后marker移动（显示新位置）
3. InfoWindow弹出显示地址
4. 底部面板显示地址信息
5. 完整的位置提醒保存后的列表展示

---

**测试时间**: [待测试]  
**测试人员**: [待填写]  
**测试结果**: [待填写]  
**发现问题**: [待填写]

