# 定位上报签名问题修复说明

## 📋 问题描述

### 1️⃣ 原始问题
```
服务器响应: {"code":41000,"msg":"公共参数sign未传","time":1759843576,"data":{}}
```

### 2️⃣ 问题根因
Native（Kotlin）定位上报代码缺少 API 签名机制，导致后端拒绝请求。

---

## ✅ 修复内容

### 1. 添加签名生成逻辑

#### 文件：`android/app/src/main/kotlin/com/yuluo/kissu/LocationReportService.kt`

**新增方法：**

1. **`generateSign()`** - 生成 API 签名
   - 收集所有业务参数（header + body）
   - 按 key 名 ASCII 升序排序
   - 拼接所有 value
   - 追加密钥 `TYXHTRrGeP8xy095q0iY`
   - MD5 哈希，转大写

2. **`md5()`** - MD5 哈希计算

3. **`getDeviceId()`** - 获取设备 ID（Android ID）

**签名算法示例：**

```kotlin
// 1. 收集参数
val params = {
    "channel": "kissu_android",
    "deviceid": "abc123...",
    "locations": "[{...}]",
    "pkg": "com.yuluo.kissu",
    "token": "eyJ0eXAi...",
    "userid": "1208",
    "version": "1.0.0"
}

// 2. 排序 key
sortedKeys = ["channel", "deviceid", "locations", "pkg", "token", "userid", "version"]

// 3. 拼接 value + 密钥
signString = "kissu_androidabc123...[{...}]com.yuluo.kissueyJ0eXAi...12081.0.0TYXHTRrGeP8xy095q0iY"

// 4. MD5 哈希
sign = MD5(signString).toUpperCase()
```

---

### 2. 更新请求头

#### 新增必要的业务 header：

| Header    | 说明           | 示例值              |
|-----------|----------------|---------------------|
| `token`   | 用户登录凭证   | `eyJ0eXAi...`       |
| `userid`  | 用户 ID        | `1208`              |
| `version` | 应用版本       | `1.0.0`             |
| `pkg`     | 包名           | `com.yuluo.kissu`   |
| `deviceid`| 设备 ID        | Android ID          |
| `channel` | 渠道标识       | `kissu_android`     |
| `timestamp` | 时间戳       | `1759843576000`     |
| `sign`    | API 签名       | `A1B2C3D4...`       |

---

### 3. 环境配置优化

#### 文件：`lib/network/tools/config/app_configN.dart`

**修改前：**
```dart
baseApiUrl = "http://129.204.154.113";  // 写死测试 IP
```

**修改后：**
```dart
if (serverEnvironmentTest) {
  // 测试环境
  baseApiUrl = "http://129.204.154.113";
} else {
  // 生产环境
  baseApiUrl = "https://service-api.ikissu.cn";
}
```

---

### 4. 网络安全配置

#### 文件：`android/app/src/main/res/xml/network_security_config.xml`

**添加测试 IP 的 HTTP 许可：**
```xml
<domain-config cleartextTrafficPermitted="true">
    ...
    <!-- 测试服务器 IP -->
    <domain includeSubdomains="true">129.204.154.113</domain>
</domain-config>
```

---

## 🔍 调试日志

修复后的日志输出（便于排查问题）：

```
D/LocationReportService: 🚀 开始上报定位数据
D/LocationReportService: 📡 API地址: https://service-api.ikissu.cn/location/report
D/LocationReportService: 📦 上报数据: [{"longitude":"120.220242",...}]
D/LocationReportService: 🔐 签名参数汇总: {channel=kissu_android, deviceid=abc123, locations=[...], pkg=com.yuluo.kissu, token=eyJ0eXAi..., userid=1208, version=1.0.0}
D/LocationReportService: 🔐 排序后的key: [channel, deviceid, locations, pkg, token, userid, version]
D/LocationReportService: 🔐 拼接参数 channel = kissu_android
D/LocationReportService: 🔐 拼接参数 deviceid = abc123
D/LocationReportService: 🔐 拼接参数 locations = [...]
D/LocationReportService: 🔐 拼接参数 pkg = com.yuluo.kissu
D/LocationReportService: 🔐 拼接参数 token = eyJ0eXAi...
D/LocationReportService: 🔐 拼接参数 userid = 1208
D/LocationReportService: 🔐 拼接参数 version = 1.0.0
D/LocationReportService: 🔐 完整签名字符串: kissu_androidabc123...[...]...TYXHTRrGeP8xy095q0iY
D/LocationReportService: 🔐 最终签名: A1B2C3D4E5F6...
D/LocationReportService: 🔐 签名已生成: A1B2C3D4E5F6...
D/LocationReportService: 📡 HTTP响应码: 200
D/LocationReportService: 📡 服务器响应: {"code":200,"msg":"success","data":{}}
D/LocationReportService: ✅ 定位上报成功
```

---

## 🎯 签名参数对照表

### Flutter 签名中包含的业务 header：

根据 `lib/network/utils/signature_utils.dart` 第 74-81 行：

```dart
const businessHeaders = {
  'channel',   // ✅ 已添加
  'version',   // ✅ 已添加
  'deviceid',  // ✅ 已添加
  'pkg',       // ✅ 已添加
  'token',     // ✅ 已添加
  'userid',    // ✅ 已添加
};
```

### 排除的系统 header：

```dart
const systemHeaders = {
  'content-type',
  'content-length',
  'user-agent',
  'accept',
  'accept-encoding',
  'connection',
  'host',
  'timestamp',  // ⚠️ 不参与签名
  'nonce',
  'sign',       // ⚠️ 签名本身不参与签名
};
```

---

## 📊 测试步骤

### 1. 编译运行
```bash
flutter run
```

### 2. 查看日志
```bash
adb logcat | grep -E "LocationReportService|ForegroundLocationService"
```

### 3. 预期结果

**成功响应：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

**失败响应（如果签名错误）：**
```json
{
  "code": 41000,
  "msg": "公共参数sign未传"
}
```

或

```json
{
  "code": 41001,
  "msg": "签名验证失败"
}
```

---

## 🔧 常见问题排查

### Q1: 仍然提示"公共参数sign未传"？

**可能原因：**
- `sign` header 未正确添加
- header 名称大小写不对（应为小写 `sign`）

**解决方案：**
检查日志中的 "🔐 签名已生成" 是否输出，以及请求头中是否包含 `sign`。

---

### Q2: 提示"签名验证失败"？

**可能原因：**
1. 签名算法不对
2. 参数收集不完整
3. 排序规则错误
4. 密钥不对

**解决方案：**
对比 Flutter 和 Kotlin 的签名生成逻辑：

**Flutter:**
```dart
// lib/network/utils/signature_utils.dart
SignatureUtils.generateSignature(options: options)
```

**Kotlin:**
```kotlin
// LocationReportService.kt
generateSign(headers, bodyParams)
```

确保两者的：
- 参数收集规则一致
- 排序规则一致（ASCII 升序）
- 拼接规则一致（只拼接 value）
- 密钥一致（`TYXHTRrGeP8xy095q0iY`）

---

### Q3: HTTP 连接失败？

**可能原因：**
- 测试环境使用 HTTP，但 Android 9+ 默认禁止明文流量
- 网络不通

**解决方案：**
1. 检查 `network_security_config.xml` 是否正确配置
2. 检查网络连接
3. 检查防火墙

---

## 📝 修改文件清单

1. ✅ `android/app/src/main/kotlin/com/yuluo/kissu/LocationReportService.kt`
   - 新增 `generateSign()` 方法
   - 新增 `md5()` 方法
   - 新增 `getDeviceId()` 方法
   - 更新 `sendLocationToServer()` 方法

2. ✅ `lib/network/tools/config/app_configN.dart`
   - 根据环境自动切换 API 地址

3. ✅ `android/app/src/main/res/xml/network_security_config.xml`
   - 添加测试 IP 的 HTTP 许可

4. ✅ `lib/main.dart`
   - 添加 `AppConfigN.configuration()` 初始化调用

---

## 🎉 总结

此次修复主要解决了 Native 定位上报缺少 API 签名的问题。通过在 Kotlin 代码中实现与 Flutter 相同的签名算法，确保了 Native 上报与 Flutter 上报的一致性。

**关键改进：**
1. ✅ 实现完整的签名生成逻辑
2. ✅ 添加所有必要的业务 header
3. ✅ 环境配置自动切换
4. ✅ 详细的调试日志输出

---

## 📅 修改日期
2025-10-07

## 👤 修改人
AI Assistant















