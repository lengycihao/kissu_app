# 🚀 后台定位和开机自启动 - 快速测试指南

## ⚡ 5分钟快速测试

### 1️⃣ 编译运行 (已完成)

```bash
flutter run
```

等待编译完成，APP 安装到手机。

---

### 2️⃣ 测试白名单引导功能

#### 方法 1: 使用测试页面（推荐）

1. **添加路由** (如果还没有)

   在 `lib/main.dart` 或路由配置中添加：
   
   ```dart
   import 'pages/test/whitelist_test_page.dart';
   
   GetPage(
     name: '/whitelist_test',
     page: () => WhitelistTestPage(),
   ),
   ```

2. **访问测试页面**

   在 APP 中导航到 `/whitelist_test`，或者临时添加一个按钮：
   
   ```dart
   ElevatedButton(
     onPressed: () => Get.toNamed('/whitelist_test'),
     child: Text('测试白名单引导'),
   )
   ```

3. **点击测试按钮**
   - 查看设备信息
   - 点击 "显示完整引导对话框"
   - 点击 "去设置" 看是否能跳转

#### 方法 2: 在现有代码中调用

在任何页面添加：

```dart
import 'package:kissu_app/utils/whitelist_helper.dart';

// 显示引导对话框
ElevatedButton(
  onPressed: () => WhitelistHelper.showGuidanceDialog(),
  child: Text('显示白名单引导'),
)
```

---

### 3️⃣ 测试手动杀掉后自动重启

**步骤**:

1. ✅ **启动定位服务** 
   - 在 APP 中点击 "开始定位"
   - 确认通知栏显示 "Kissu定位服务"

2. ✅ **杀掉 APP**
   - 在任务管理器中上滑关闭 APP
   - 或者: 设置 → 应用管理 → Kissu → 强行停止

3. ✅ **等待 5-10 秒**

4. ✅ **检查结果**
   - 通知栏应该仍然显示 "Kissu定位服务"
   - 重新打开 APP，定位服务仍在运行

**查看日志**:
```bash
adb logcat | grep "ForegroundLocationService"
```

**预期日志**:
```
D/ForegroundLocationService: onStartCommand called
D/ForegroundLocationService: Service will restart automatically (START_STICKY)
```

---

### 4️⃣ 测试开机自启动

**步骤**:

1. ✅ **启动定位服务** (确保定位正在运行)

2. ✅ **重启手机**

3. ✅ **解锁屏幕后等待 10-30 秒** (不要手动打开 APP)

4. ✅ **检查通知栏**
   - 应该看到 "Kissu定位服务" 通知

**查看日志**:
```bash
adb logcat | grep "BootCompletedReceiver"
```

**预期日志**:
```
D/BootCompletedReceiver: 收到系统广播: android.intent.action.BOOT_COMPLETED
D/BootCompletedReceiver: 用户上次已启用定位服务，准备自动启动...
D/BootCompletedReceiver: ✅ 前台定位服务启动成功（开机自启动）
```

---

### 5️⃣ 测试息屏后继续定位

**步骤**:

1. ✅ **启动定位服务**

2. ✅ **锁屏** (按电源键)

3. ✅ **等待 3-5 分钟**

4. ✅ **解锁查看日志**

**查看日志**:
```bash
adb logcat | grep "位置上报"
```

**预期**:
即使在息屏期间，也应该看到：
```
I/flutter: 📍 位置上报触发: 定时上报(间隔60秒)
I/flutter: ✅ 位置上报成功
```

---

## 🔧 常见问题

### ❓ 杀掉后服务没重启

**原因**: 厂商限制太严格

**解决**: 
1. 使用白名单引导功能
2. 手动设置：
   - **小米**: 设置 → 应用设置 → 应用管理 → Kissu → 自启动
   - **华为**: 设置 → 应用和服务 → 应用启动管理 → Kissu
   - **OPPO**: 设置 → 应用管理 → 应用列表 → Kissu → 自启动

---

### ❓ 开机后没有自动启动

**检查**:
1. 是否启用了自启动权限
2. 查看日志，确认收到了开机广播

**日志命令**:
```bash
adb logcat | grep "BootCompletedReceiver"
```

---

### ❓ 息屏后定位停止

**原因**: 没有后台定位权限

**解决**:
设置 → 应用管理 → Kissu → 权限 → 位置 → **始终允许**

---

## 📊 测试检查清单

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 白名单引导功能 | ☐ | 能否显示对话框 |
| 跳转到设置页面 | ☐ | 点击"去设置"是否跳转 |
| 手动杀掉后重启 | ☐ | 通知是否保留 |
| 开机自动启动 | ☐ | 重启后是否自动启动 |
| 息屏后继续定位 | ☐ | 锁屏后是否继续上报 |

---

## 🎯 集成到实际使用

### 在首次启动定位时引导用户

在你的定位服务启动代码中添加：

```dart
import 'package:kissu_app/utils/whitelist_helper.dart';

Future<void> startLocationService() async {
  // 1. 首次启动时检查白名单
  bool needsGuidance = await WhitelistHelper.needsWhitelistGuidance();
  
  if (needsGuidance) {
    // 显示引导（首次才显示）
    await WhitelistHelper.checkAndShowGuidance();
  }
  
  // 2. 启动定位服务
  await _actualStartLocation();
  
  // 3. 保存状态（用于开机自启动）
  // 这部分需要在原生层实现
}
```

---

## 🔍 调试技巧

### 实时查看日志

```bash
# 查看所有相关日志
adb logcat | grep -E "BootCompletedReceiver|ForegroundLocationService|DeviceWhitelistHelper"

# 只看定位相关
adb logcat | grep "位置"

# 只看服务启动
adb logcat | grep "ForegroundLocationService"
```

### 模拟开机广播（测试用）

```bash
# 模拟开机完成
adb shell am broadcast -a android.intent.action.BOOT_COMPLETED

# 查看是否收到
adb logcat | grep "BootCompletedReceiver"
```

---

## 📞 需要帮助？

如果测试失败，请提供：
1. 设备型号和系统版本
2. 日志输出 (`adb logcat`)
3. 测试步骤截图

更详细的测试指南请查看：`测试指南_后台定位和开机自启动.md`

---

**测试愉快！** 🎉



