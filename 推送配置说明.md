# 腾讯云IM推送配置说明

## 已完成的工作

✅ 已添加推送插件依赖：`tencent_cloud_chat_push`
✅ 已在 `TencentIMService` 中集成推送注册代码
✅ 已实现推送通知点击处理逻辑
✅ 已在退出登录时添加反注册推送

## 需要配置的参数

在 `lib/services/tencent_im_service.dart` 的 `_registerPushService()` 方法中，只需要配置一个参数：

### appKey（必填）

**位置**：第1383行
```dart
const String? appKey = null; // 🔥 需要从控制台获取并配置
```

**获取方式**：
1. 登录 [腾讯云IM控制台](https://console.cloud.tencent.com/im)
2. 进入您的应用（SDKAppID: 1600095370）
3. 在"应用配置" -> "离线推送配置"中获取"客户端密钥"
4. 将获取到的密钥配置到代码中

**示例**：
```dart
const String? appKey = "your_app_key_here";
```

**注意**：当前项目只支持Android平台，iOS相关配置已移除。

## Android推送通道配置

Android平台支持多种推送通道，需要在**腾讯云IM控制台**配置（代码中不需要额外配置）：

1. **厂商通道**（推荐）：配置小米、华为、OPPO、vivo、魅族等厂商推送通道
   - 在腾讯云IM控制台配置各厂商的AppID、AppKey、AppSecret
   - 配置完成后，推送会自动根据设备品牌选择合适的通道
   - **详细配置步骤请参考**：`腾讯云IM厂商通道配置指南.md`

2. **FCM通道**：配置Firebase Cloud Messaging作为兜底
   - 在腾讯云IM控制台配置FCM Server Key
   - 需要下载 `google-services.json` 文件到 `android/app/` 目录
   - **详细配置步骤请参考**：`腾讯云IM厂商通道配置指南.md`

3. **本机通道**：作为最后兜底方案，无需额外配置

**推荐配置顺序**：
1. 优先配置厂商通道（小米、华为、OPPO、vivo等，覆盖80%+用户）
2. 配置FCM通道作为兜底
3. 本机通道会自动作为最后兜底

**重要**：所有推送通道配置都在腾讯云IM控制台完成，代码中只需要配置 `appKey` 即可！

## 配置完成后

配置完 `appKey` 后：

1. 重新编译运行应用
2. 登录IM账号后，推送服务会自动注册
3. 测试离线推送：
   - 将App切换到后台或完全关闭
   - 让其他用户发送消息
   - 应该能收到推送通知
4. 测试推送点击：
   - 点击推送通知
   - 应该能跳转到聊天页面

## 注意事项

1. **隐私合规**：推送服务注册在用户同意隐私政策后进行（已实现）
2. **权限申请**：Android需要申请通知权限（项目中已有权限管理）
3. **证书配置**：iOS和Android的推送证书需要在腾讯云IM控制台正确配置
4. **测试环境**：开发环境可以使用测试证书，生产环境需要使用生产证书

## 故障排查

如果推送不工作，检查以下几点：

1. ✅ `appKey` 是否已配置且正确
2. ✅ 腾讯云IM控制台Android推送通道是否已正确配置（FCM或厂商通道）
3. ✅ 设备是否已授予通知权限
4. ✅ 查看日志：推送注册是否成功（搜索 "推送服务注册成功（Android平台）"）
5. ✅ 检查设备网络连接是否正常
6. ✅ 确认设备是否支持推送服务（某些模拟器可能不支持）

## 参考文档

- 腾讯云IM推送插件API文档：`api.md`
- 腾讯云IM官方文档：https://cloud.tencent.com/document/product/269

