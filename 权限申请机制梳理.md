# Kissu App 权限申请机制完整梳理

## 📋 目录
1. [权限类型总览](#权限类型总览)
2. [权限申请架构](#权限申请架构)
3. [权限申请触发点](#权限申请触发点)
4. [权限配置说明](#权限配置说明)

---

## 1. 权限类型总览

应用中涉及的所有权限类型（定义在 `lib/services/permission_service.dart`）：

### 核心权限枚举 `PermissionType`
```dart
enum PermissionType {
  location,           // 位置权限（前台定位）
  locationAlways,     // 后台位置权限（始终允许定位）
  notification,       // 通知权限
  battery,            // 电池优化权限（后台运行）
  usage,              // 使用情况访问权限（应用使用时长统计）
  photos,             // 相册权限
  camera,             // 相机权限
}
```

### Android Manifest 中声明的权限

| 权限 | 用途 | 运行时申请 |
|------|------|----------|
| `CAMERA` | 拍照上传头像、扫描二维码 | ✅ 是 |
| `READ_EXTERNAL_STORAGE` | 读取图片文件 | ✅ 是 (Android 12-) |
| `READ_MEDIA_IMAGES` | 读取媒体图片 | ✅ 是 (Android 13+) |
| `READ_MEDIA_VISUAL_USER_SELECTED` | 部分照片访问 | ✅ 是 (Android 16+) |
| `ACCESS_COARSE_LOCATION` | 粗略定位 | ✅ 是 |
| `ACCESS_FINE_LOCATION` | 精确定位 | ✅ 是 |
| `ACCESS_BACKGROUND_LOCATION` | 后台定位 | ✅ 是 |
| `POST_NOTIFICATIONS` | 发送通知 | ✅ 是 (Android 13+) |
| `PACKAGE_USAGE_STATS` | 应用使用统计 | ✅ 特殊权限（需跳转设置） |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 忽略电池优化 | ✅ 特殊权限（需跳转设置） |
| `INTERNET` | 网络访问 | ❌ 否 |
| `ACCESS_NETWORK_STATE` | 网络状态 | ❌ 否 |
| `ACCESS_WIFI_STATE` | WiFi状态 | ❌ 否 |
| `RECEIVE_BOOT_COMPLETED` | 开机自启 | ❌ 否 |
| `FOREGROUND_SERVICE` | 前台服务 | ❌ 否 |
| `FOREGROUND_SERVICE_LOCATION` | 前台定位服务 | ❌ 否 |

---

## 2. 权限申请架构

### 2.1 核心服务类

#### `PermissionService` (lib/services/permission_service.dart)
**职责**：权限检查和请求的中央服务

- **检查权限状态**：
  - `isLocationPermissionGranted()` - 位置权限
  - `isLocationAlwaysPermissionGranted()` - 后台位置权限
  - `isNotificationPermissionGranted()` - 通知权限
  - `isBatteryOptimizationDisabled()` - 电池优化权限
  - `isUsageAccessGranted()` - 使用情况访问权限
  - `isPhotosPermissionGranted()` - 相册权限
  - `isCameraPermissionGranted()` - 相机权限

- **请求权限**：
  - `requestLocationPermission()` - 请求位置权限
  - `requestLocationAlwaysPermission()` - 请求后台位置权限
  - `requestNotificationPermission()` - 请求通知权限
  - `requestBatteryOptimizationPermission()` - 请求电池优化权限
  - `requestUsageAccessPermission()` - 请求使用情况访问权限
  - `requestPhotosPermission()` - 请求相册权限
  - `requestCameraPermission()` - 请求相机权限

- **智能批量申请**：
  - `requestPermissionsIntelligently()` - 智能请求所有权限（按顺序）
  - `checkAllPermissions()` - 批量检查所有权限状态

- **跳转系统设置**：
  - `openLocationSettings()` - 跳转定位设置
  - `openNotificationSettings()` - 跳转通知设置
  - `openBatteryOptimizationSettings()` - 跳转电池优化设置
  - `openUsageAccessSettings()` - 跳转使用情况访问设置
  - `openAppSettings()` - 跳转应用设置

#### `LocationPermissionManager` (lib/services/location_permission_manager.dart)
**职责**：定位权限专用管理器（带自定义弹窗）

**核心流程**：
1. 检查当前权限状态
2. 如果未授权，先显示自定义弹窗（`LocationPermissionDialog`）
3. 用户同意后，再弹出系统权限申请
4. 如果被永久拒绝，引导用户跳转到系统设置

**防重复弹窗机制**：
- `_isShowingDialog` 标志防止重复弹窗

#### `LocationPermissionService` (lib/services/location_permission_service.dart)
**职责**：定位权限的登录后触发逻辑

- `requestLocationPermissionAfterLogin()` - 首次登录后请求定位权限
- `shouldRequestLocationPermission()` - 检查是否需要请求权限（通过 SharedPreferences 记录）
- `markLocationPermissionRequested()` - 标记已请求过

#### `PermissionHelper` (lib/utils/permission_helper.dart)
**职责**：通过 MethodChannel 调用原生方法打开系统设置

- `openLocationSettings()` - 打开定位设置
- `openNotificationSettings()` - 打开通知设置
- `openBatteryOptimizationSettings()` - 打开电池优化设置
- `openUsageAccessSettings()` - 打开使用情况访问设置
- `openWifiSettings()` - 打开WiFi设置
- `openAppSettings()` - 打开应用设置

### 2.2 自定义权限弹窗

| 弹窗类 | 用途 | 位置 |
|--------|------|------|
| `LocationPermissionDialog` | 定位权限申请弹窗 | `lib/widgets/dialogs/location_permission_dialog.dart` |
| `PartnerLocationPermissionDialog` | 对方定位权限提醒弹窗 | `lib/widgets/dialogs/partner_location_permission_dialog.dart` |
| `SelfNotificationPermissionDialog` | 自己通知权限提醒弹窗 | `lib/widgets/dialogs/self_notification_permission_dialog.dart` |
| `PermissionRequestDialog` | 通用权限请求弹窗 | `lib/widgets/dialogs/permission_request_dialog.dart` |

---

## 3. 权限申请触发点

### 🔥 3.0 启动页自动权限申请（首次下载）

**触发时机**：用户首次下载APP，在启动页同意隐私政策后

**申请流程**：
```
用户首次启动APP
  ↓
显示隐私政策弹窗
  ↓
用户点击"同意并继续"
  ↓
初始化SDK（PrivacyComplianceManager.agreeToPrivacyPolicy()）
  ↓
🔥 立即申请关键权限（SplashPage._requestEssentialPermissionsAfterAgreement()）
  ↓
├─ 申请通知权限（JPushService.requestNotificationPermission()）
│   └─ 弹出系统通知权限申请弹窗
│
└─ 网络权限自动授予（已在AndroidManifest.xml中声明）
  ↓
继续应用启动流程
```

**申请的权限**：
1. ✅ **通知权限** (`POST_NOTIFICATIONS`) - Android 13+ 必需，通过极光推送服务申请
2. ✅ **网络权限** (`INTERNET`) - 已在Manifest声明，自动授予，无需运行时申请

**实现位置**：
- 文件：`lib/pages/splash/splash_page.dart`
- 方法：`_requestEssentialPermissionsAfterAgreement()`

**优势**：
- ✅ 用户体验友好：在用户同意隐私政策后立即申请，符合隐私合规要求
- ✅ 时机合理：用户刚同意隐私政策，对权限申请有心理预期
- ✅ 不阻塞流程：即使权限申请失败，也不影响应用启动

---

### 3.1 定位权限 (Location Permission)

#### 触发场景

| 场景 | 触发位置 | 触发方法 | 说明 |
|------|----------|----------|------|
| **首次登录** | `LocationPermissionService.requestLocationPermissionAfterLogin()` | 登录成功后自动触发 | 首次登录时才会触发，通过 SharedPreferences 记录 |
| **首页加载** | `HomeController._requestLocationPermissionOnHomePage()` | 首页初始化时 | 检查GPS状态异常时触发 |
| **首页手动开启定位** | `HomeController._requestLocationPermissionAndStartService()` | 用户点击开启定位按钮 | 用户主动触发 |
| **定位页面** | `LocationV2Controller._requestLocationPermissionAndStartService()` | 定位页面初始化 | 检查GPS状态后触发 |
| **轨迹页面** | `TrackController._requestLocationPermissionAndLoadData()` | 轨迹页面初始化 | 加载数据前检查权限 |
| **防偷拍页面** | `AntiSpyController` | 开始扫描时 | 扫描网络时需要位置信息 |
| **系统权限设置页** | `SystemPermissionController.onPermissionTap()` | 用户点击"去设置" | 手动跳转系统设置 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户点击权限项 | 手动申请权限 |

#### 调用链路示例（首次登录）

```
登录成功
  ↓
LocationPermissionService.requestLocationPermissionAfterLogin()
  ↓
LocationPermissionManager.requestLocationPermission()
  ↓
显示自定义弹窗 LocationPermissionDialog
  ↓ (用户同意)
Permission.location.request() (系统弹窗)
  ↓ (权限获取成功)
SimpleLocationService.startLocation() (启动定位服务)
```

### 3.2 后台定位权限 (Background Location Permission)

#### 触发场景

| 场景 | 触发位置 | 说明 |
|------|----------|------|
| **智能批量申请** | `PermissionService.requestPermissionsIntelligently()` | 在获取前台定位权限后自动请求 |
| **系统权限设置页** | `SystemPermissionController.onPermissionTap()` | 用户手动跳转设置 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户手动申请 |

**注意**：Android 需要先获取前台定位权限，才能请求后台定位权限。

### 3.3 通知权限 (Notification Permission)

#### 触发场景

| 场景 | 触发位置 | 触发方法 | 说明 |
|------|----------|----------|------|
| **🔥 启动页隐私协议同意后** | `SplashPage._requestEssentialPermissionsAfterAgreement()` | 用户同意隐私政策后 | **首次下载时自动触发，立即申请通知权限** |
| **极光推送初始化** | `JPushService.requestNotificationPermission()` | JPush初始化后 | 自动检查并申请通知权限 |
| **智能批量申请** | `PermissionService.requestPermissionsIntelligently()` | 批量申请权限时 | 包含在智能申请流程中 |
| **系统权限设置页** | `SystemPermissionController.onPermissionTap()` | 用户手动跳转设置 | 跳转到系统通知设置 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户手动申请 | 手动申请通知权限 |
| **应用生命周期监听** | `AppLifecycleService._checkNotificationPermissionChange()` | 应用从后台恢复时 | 检测通知权限变化并上报 |

### 3.4 相机权限 (Camera Permission)

#### 触发场景

| 场景 | 触发位置 | 触发方法 | 说明 |
|------|----------|----------|------|
| **拍照（头像上传）** | `MediaPickerUtil.takePhoto()` | 用户选择拍照 | 上传头像时 |
| **拍照（聊天图片）** | `ImageDialogUtil` | 聊天页面发送图片 | 选择拍照时 |
| **设置用户信息** | `InfoSettingController` | 用户设置头像 | 选择拍照时 |
| **情侣信息设置** | `LoveInfoController` | 设置情侣头像 | 选择拍照时 |
| **二维码扫描** | `QrScanPage._checkPermissionAndRefresh()` | 打开扫码页面 | 页面初始化时自动检查 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户手动申请 | 手动申请相机权限 |

**调用链路示例（二维码扫描）**：
```
QrScanPage 初始化
  ↓
_checkPermissionAndRefresh()
  ↓
Permission.camera.status 检查
  ↓ (如果未授权)
Permission.camera.request() (系统弹窗)
  ↓ (如果永久拒绝)
显示引导弹窗，引导用户跳转系统设置
```

### 3.5 相册权限 (Photos Permission)

#### 触发场景

| 场景 | 触发位置 | 触发方法 | 说明 |
|------|----------|----------|------|
| **从相册选择图片** | `MediaPickerUtil.pickImageFromGallery()` | 用户选择相册 | 上传头像、聊天图片等 |
| **设置用户信息** | `InfoSettingController` | 设置头像时选择相册 | 选择相册时 |
| **情侣信息设置** | `LoveInfoController` | 设置情侣头像 | 选择相册时 |
| **聊天页面** | `ImageDialogUtil` | 发送图片时选择相册 | 选择相册时 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户手动申请 | 手动申请相册权限 |

**注意**：
- Android 13+ 使用系统 Photo Picker，无需申请 `READ_MEDIA_IMAGES` 权限
- Android 12- 需要申请 `READ_EXTERNAL_STORAGE` 权限

### 3.6 电池优化权限 (Battery Optimization)

#### 触发场景

| 场景 | 触发位置 | 说明 |
|------|----------|------|
| **智能批量申请** | `PermissionService.requestPermissionsIntelligently()` | 批量申请权限时包含 |
| **系统权限设置页** | `SystemPermissionController.onPermissionTap()` | 用户手动跳转设置 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户手动申请 |

**用途**：确保应用在后台不被系统杀死，保证定位服务和推送服务持续运行。

### 3.7 使用情况访问权限 (Usage Stats Permission)

#### 触发场景

| 场景 | 触发位置 | 触发方法 | 说明 |
|------|----------|----------|------|
| **使用报告页面** | `UsageReportController` | 查看使用报告时 | 需要读取应用使用时长 |
| **屏幕使用服务** | `ScreenUsageService.requestPermission()` | 服务初始化时 | 需要读取屏幕使用时长 |
| **智能批量申请** | `PermissionService.requestPermissionsIntelligently()` | 批量申请权限时包含 | 批量申请流程 |
| **系统权限设置页** | `SystemPermissionController.onPermissionTap()` | 用户手动跳转设置 | 跳转到系统设置页面 |
| **权限设置页** | `PermissionSettingPage._requestPermission()` | 用户手动申请 | 手动申请权限 |

**注意**：这是一个特殊权限，无法通过系统弹窗直接申请，必须跳转到系统设置页面让用户手动授权。

---

## 4. 权限配置说明

### 4.1 Android 原生配置

#### MethodChannel 配置 (MainActivity.kt)

定义了以下 MethodChannel：

| Channel名称 | 用途 | 方法 |
|------------|------|------|
| `app.location/settings` | 权限设置跳转 | `openLocationSettings`, `openNotificationSettings`, `openBatteryOptimizationSettings`, `openUsageAccessSettings`, `openWifiSettings`, `openAppSettings` |
| `app.wechat/launch` | 企业微信客服 | `openWeComKfWithParams` |
| `app.notification/check` | 通知权限检查 | `checkNotificationPermission` |

#### 关键原生实现

1. **定位设置跳转**：
```kotlin
Intent(Settings.ACTION_LOCATION_SOURCE_SETTINGS)
```

2. **通知设置跳转**：
```kotlin
Intent(Settings.ACTION_APP_NOTIFICATION_SETTINGS)
  .putExtra(Settings.EXTRA_APP_PACKAGE, packageName)
```

3. **电池优化设置跳转**：
```kotlin
Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS)
  .setData(Uri.parse("package:$packageName"))
```

4. **使用情况访问设置跳转**：
```kotlin
Intent(Settings.ACTION_USAGE_ACCESS_SETTINGS)
```

### 4.2 权限申请流程图

#### 标准权限申请流程
```
开始
  ↓
检查权限状态
  ↓
已授权? → 是 → 返回成功
  ↓ 否
被永久拒绝? → 是 → 显示引导弹窗 → 跳转系统设置
  ↓ 否
显示自定义弹窗（可选）
  ↓
用户同意? → 否 → 返回失败
  ↓ 是
弹出系统权限申请
  ↓
授权成功? → 是 → 返回成功
  ↓ 否
返回失败
```

#### 定位权限特殊流程（带自定义弹窗）
```
开始
  ↓
检查权限状态
  ↓
已授权? → 是 → 返回成功
  ↓ 否
被永久拒绝? → 是 → 显示自定义弹窗 → 跳转系统设置
  ↓ 否
显示自定义弹窗 (LocationPermissionDialog)
  ↓
用户同意? → 否 → 返回失败
  ↓ 是
弹出系统权限申请
  ↓
授权成功? → 是 → 启动定位服务 → 返回成功
  ↓ 否
显示Toast提示 → 返回失败
```

### 4.3 系统权限设置页面

**页面路径**：`lib/pages/mine/sub_pages/system_permission_page.dart`

**展示的权限项**：
1. ✅ 开启实时定位 (`PermissionType.location`)
2. ✅ 允许后台运行 (`PermissionType.battery`)
3. ✅ 开启通知提醒 (`PermissionType.notification`)
4. ✅ 允许获取应用使用权限 (`PermissionType.usage`)

**功能**：
- 实时显示权限状态（已开启/未开启）
- 点击"去设置"按钮跳转到对应的系统设置页面
- 从设置页面返回后自动刷新权限状态（通过 `WidgetsBindingObserver` 监听应用生命周期）

---

## 5. 关键注意事项

### 5.1 Android 版本适配

| Android版本 | 特殊处理 |
|------------|---------|
| Android 13+ (API 33+) | 需要 `POST_NOTIFICATIONS` 权限（通知） |
| Android 13+ (API 33+) | 使用 `READ_MEDIA_IMAGES` 替代 `READ_EXTERNAL_STORAGE` |
| Android 13+ (API 33+) | 支持系统 Photo Picker，无需申请相册权限 |
| Android 16+ | 支持 `READ_MEDIA_VISUAL_USER_SELECTED` 部分照片权限 |
| Android 10+ (API 29+) | 需要前台服务类型声明 `FOREGROUND_SERVICE_LOCATION` |
| Android 11+ (API 30+) | 后台定位权限需要单独申请 `ACCESS_BACKGROUND_LOCATION` |

### 5.2 权限依赖关系

1. **后台定位依赖前台定位**：
   - 必须先获取 `ACCESS_FINE_LOCATION`
   - 才能申请 `ACCESS_BACKGROUND_LOCATION`

2. **定位服务依赖权限**：
   - 定位服务启动前必须确保定位权限已授权
   - 后台定位提醒功能依赖后台定位权限

3. **使用报告依赖使用情况访问权限**：
   - 必须先获取 `PACKAGE_USAGE_STATS` 权限
   - 才能读取应用使用时长数据

### 5.3 防重复弹窗机制

- `LocationPermissionManager` 使用 `_isShowingDialog` 标志防止重复弹窗
- `SystemPermissionController` 监听应用生命周期，从设置返回时自动刷新权限状态

### 5.4 隐私合规

- ✅ 首次登录时才申请定位权限（通过 SharedPreferences 记录）
- ✅ 权限申请前先显示自定义弹窗说明用途
- ✅ 极光推送初始化延迟到用户同意隐私政策后
- ✅ 高德地图初始化延迟到用户同意隐私政策后

---

## 6. 快速查询表

### 我想知道某个权限在哪里被申请？

| 权限 | 主要触发点 | 服务类 |
|------|----------|--------|
| 定位权限 | 首次登录、首页、定位页、轨迹页 | `LocationPermissionManager` |
| 后台定位权限 | 智能批量申请、系统设置页 | `PermissionService` |
| 通知权限 | 极光推送初始化、系统设置页 | `JPushService`, `PermissionService` |
| 相机权限 | 拍照上传头像、二维码扫描 | `MediaPickerUtil`, `QrScanPage` |
| 相册权限 | 选择相册图片 | `MediaPickerUtil` |
| 电池优化权限 | 智能批量申请、系统设置页 | `PermissionService` |
| 使用情况访问权限 | 使用报告页、系统设置页 | `ScreenUsageService`, `PermissionService` |

### 我想知道权限申请的调用链路？

查看对应权限的"触发场景"章节，里面有详细的调用链路说明。

### 我想修改权限申请弹窗？

自定义弹窗位于 `lib/widgets/dialogs/` 目录下：
- `location_permission_dialog.dart` - 定位权限弹窗
- `partner_location_permission_dialog.dart` - 对方定位权限提醒
- `self_notification_permission_dialog.dart` - 通知权限提醒
- `permission_request_dialog.dart` - 通用权限请求弹窗

### 我想添加新的权限？

1. 在 `PermissionType` 枚举中添加新权限类型
2. 在 `PermissionService` 中添加检查和请求方法
3. 在 `AndroidManifest.xml` 中声明权限
4. 在需要的页面触发权限申请

---

## 7. 总结

Kissu App 的权限申请机制设计完善，具有以下特点：

✅ **分层清晰**：PermissionService 提供统一的权限管理接口
✅ **流程规范**：先显示自定义弹窗说明用途，再申请系统权限
✅ **防重复弹窗**：通过标志位和生命周期监听避免重复弹窗
✅ **智能申请**：支持批量智能申请，按依赖关系顺序请求
✅ **用户友好**：权限被拒绝时提供明确引导，跳转系统设置
✅ **隐私合规**：延迟初始化第三方SDK，首次登录才申请关键权限
✅ **版本适配**：针对不同Android版本做了特殊处理

**关键文件**：
- `lib/services/permission_service.dart` - 权限服务核心
- `lib/services/location_permission_manager.dart` - 定位权限管理器
- `lib/utils/permission_helper.dart` - 原生方法调用
- `android/app/src/main/kotlin/com/yuluo/kissu/MainActivity.kt` - Android原生实现
- `android/app/src/main/AndroidManifest.xml` - 权限声明

---

## 8. 最新更新（2025-10-29）

### 🔥 新增功能：启动页自动申请关键权限

**更新内容**：
- 在用户首次下载APP并同意隐私政策后，立即在启动页申请通知权限
- 网络权限自动授予（已在AndroidManifest.xml中声明）
- 权限申请不阻塞应用启动流程

**实现位置**：
- 文件：`lib/pages/splash/splash_page.dart`
- 新增方法：`_requestEssentialPermissionsAfterAgreement()`
- 调用时机：用户点击"同意并继续"后立即触发

**优势**：
1. ✅ **时机最佳**：用户刚同意隐私政策，对权限申请有心理预期
2. ✅ **符合规范**：先获取用户隐私同意，再申请敏感权限
3. ✅ **用户友好**：即使拒绝权限也不影响应用使用
4. ✅ **提高转化率**：首次启动即申请，提高通知权限授予率

**申请流程**：
```
启动页 → 隐私政策弹窗 → 用户同意 → 初始化SDK → 🔥立即申请通知权限 → 继续启动
```

**测试指南**：
详见 `启动页权限申请测试指南.md`

---

**生成时间**：2025-10-29  
**版本**：v1.0.4  
**最后更新**：2025-10-29 - 新增启动页自动权限申请功能

