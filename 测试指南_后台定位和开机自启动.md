# 📋 Kissu APP - 后台定位和开机自启动测试指南

## 🎯 测试目标

验证以下功能：
1. ✅ **手动杀掉 APP 后，定位服务自动重启**
2. ✅ **重启手机后，定位服务自动恢复**
3. ✅ **息屏后，定位继续工作**
4. ✅ **厂商白名单引导功能**

---

## 📱 测试环境要求

- **Android 版本**: Android 8.0+ (推荐 Android 10+)
- **测试设备**: 华为/小米/OPPO/VIVO 等国产手机（更严格的后台限制）
- **权限准备**: 
  - ✅ 位置权限 - **始终允许**（非常重要！）
  - ✅ 通知权限
  - ✅ 后台运行权限
  - ✅ 自启动权限

---

## 🧪 测试 1: 手动杀掉 APP 后自动重启

### 📝 测试步骤

1. **启动 APP 并开始定位**
   - 打开 Kissu APP
   - 进入首页，点击**开始定位**按钮
   - 等待前台服务启动，通知栏出现 **"Kissu定位服务"**

2. **确认定位正常工作**
   - 观察通知栏内容是否显示当前位置
   - 日志应显示：
     ```
     I/flutter: 📍 位置更新成功
     I/flutter: ✅ 位置上报成功
     ```

3. **手动杀掉 APP**
   - **方法 1**: 在任务管理器中**上滑关闭 APP**
   - **方法 2**: 在设置 → 应用管理 → Kissu → 强行停止

4. **等待 5-10 秒**
   - 系统会自动重启服务（START_STICKY 机制）

5. **检查结果**
   - ✅ 通知栏仍然显示 **"Kissu定位服务"**
   - ✅ 重新打开 APP，定位服务仍在运行
   - ✅ 位置继续上报到服务器

### ✅ 预期结果

| 检查项 | 预期 | 实际 |
|--------|------|------|
| 通知栏显示服务通知 | ✅ | ___ |
| 定位服务自动重启 | ✅ | ___ |
| 位置继续上报 | ✅ | ___ |
| APP 重新打开后状态正常 | ✅ | ___ |

### ❌ 如果失败

**可能原因**:
- 厂商的省电优化太严格
- 没有添加到**自启动白名单**
- 没有允许**后台运行**

**解决方案**:
→ 继续进行**测试 4**，使用白名单引导功能

---

## 🔄 测试 2: 重启手机后自动启动

### 📝 测试步骤

1. **启动 APP 并开始定位**
   - 确保定位服务正常运行
   - 通知栏显示定位服务

2. **重启手机**
   - 长按电源键 → 重启
   - 或者在设置中重启

3. **解锁屏幕后观察**
   - **不要手动打开 APP**
   - 等待 10-30 秒
   - 检查通知栏

4. **验证服务状态**
   - 下拉通知栏，查看是否有 **"Kissu定位服务"**
   - 打开 APP，查看定位服务是否已启动

### ✅ 预期结果

| 检查项 | 预期 | 实际 |
|--------|------|------|
| 开机后自动启动服务 | ✅ | ___ |
| 通知栏显示服务通知 | ✅ | ___ |
| APP 打开时显示定位已启动 | ✅ | ___ |
| 位置正常上报 | ✅ | ___ |

### 📊 开机日志检查

使用 `adb logcat` 查看日志：

```bash
adb logcat | grep "BootCompletedReceiver"
```

**预期日志**:
```
D/BootCompletedReceiver: 收到系统广播: android.intent.action.BOOT_COMPLETED
D/BootCompletedReceiver: 设备启动完成，准备恢复定位服务...
D/BootCompletedReceiver: 用户上次已启用定位服务，准备自动启动...
D/BootCompletedReceiver: ✅ 前台定位服务启动成功（开机自启动）
```

### ❌ 如果失败

**可能原因**:
- 没有开启**自启动权限**
- 厂商限制了开机广播接收

**解决方案**:
1. 检查 **设置 → 应用管理 → Kissu → 自启动** - 开启
2. 检查 **设置 → 电池 → 后台管理** - 允许后台运行
3. 使用白名单引导功能（测试 4）

---

## 🌙 测试 3: 息屏后继续定位

### 📝 测试步骤

1. **启动定位服务**
   - 确保定位正常工作

2. **锁屏（息屏）**
   - 按电源键锁屏
   - 等待 3-5 分钟

3. **解锁后检查**
   - 查看日志，确认在息屏期间是否有定位上报
   - 查看服务器后台，确认收到位置数据

### ✅ 预期结果

| 检查项 | 预期 | 实际 |
|--------|------|------|
| 息屏期间定位继续工作 | ✅ | ___ |
| 息屏期间位置上报 | ✅ | ___ |
| WAKE_LOCK 保持 CPU 唤醒 | ✅ | ___ |

### 📊 息屏日志检查

```bash
adb logcat | grep "位置上报"
```

**预期**:
即使在息屏期间，也应该看到：
```
I/flutter: 📍 位置上报触发: 定时上报(间隔60秒)
I/flutter: ✅ 位置上报成功
```

---

## 🔥 测试 4: 厂商白名单引导功能

### 📝 准备工作

在 Flutter 代码中添加测试按钮：

```dart
// 在某个页面添加测试按钮
import 'package:flutter/services.dart';

class WhitelistTestPage extends StatelessWidget {
  static const MethodChannel _channel = MethodChannel('kissu_app/whitelist');
  
  Future<void> testWhitelist() async {
    // 1. 获取厂商名称
    String manufacturer = await _channel.invokeMethod('getManufacturer');
    print('📱 当前设备: $manufacturer');
    
    // 2. 检查是否需要引导
    bool needsGuidance = await _channel.invokeMethod('needsWhitelistGuidance');
    print('🔍 需要白名单引导: $needsGuidance');
    
    if (needsGuidance) {
      // 3. 获取引导文本
      String guidance = await _channel.invokeMethod('getGuidanceText');
      String shortGuidance = await _channel.invokeMethod('getShortGuidance');
      
      print('📖 详细引导:\n$guidance');
      print('📝 简短引导: $shortGuidance');
      
      // 4. 显示对话框
      showDialog(
        context: context,
        builder: (context) => AlertDialog(
          title: Text('$manufacturer 手机后台设置'),
          content: SingleChildScrollView(
            child: Text(guidance),
          ),
          actions: [
            TextButton(
              onPressed: () => Navigator.pop(context),
              child: Text('稍后设置'),
            ),
            ElevatedButton(
              onPressed: () async {
                Navigator.pop(context);
                // 5. 打开白名单设置
                bool success = await _channel.invokeMethod('openWhitelistSettings');
                print('⚙️ 打开设置页面: $success');
              },
              child: Text('去设置'),
            ),
          ],
        ),
      );
    }
  }
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('白名单引导测试')),
      body: Center(
        child: ElevatedButton(
          onPressed: testWhitelist,
          child: Text('测试白名单引导'),
        ),
      ),
    );
  }
}
```

### 📝 测试步骤

1. **点击测试按钮**
   - 运行上述代码，点击 "测试白名单引导"

2. **查看控制台输出**
   ```
   📱 当前设备: HUAWEI
   🔍 需要白名单引导: true
   📖 详细引导:
   为确保Kissu能在后台持续为您提供定位服务，请按以下步骤设置：

   1. 打开【设置】→【应用和服务】→【应用启动管理】
   2. 找到【Kissu】
   3. 关闭【自动管理】
   4. 手动开启【允许后台活动】【允许自启动】【允许关联启动】
   5. 返回后检查省电策略，建议设为【性能模式】

   这样可以确保您的位置数据及时上报，保护您的安全。
   ```

3. **点击"去设置"**
   - 应该跳转到对应厂商的白名单设置页面

4. **手动设置白名单**
   - 按照引导文本进行设置

### ✅ 支持的厂商

| 厂商 | 识别名称 | 设置页面 | 状态 |
|------|---------|---------|------|
| 小米 | XIAOMI/Redmi | 自启动管理 | ✅ |
| 华为 | HUAWEI | 应用启动管理 | ✅ |
| 荣耀 | HONOR | 应用启动管理 | ✅ |
| OPPO | OPPO/Realme | 自启动管理 | ✅ |
| VIVO | VIVO | 后台高耗电 | ✅ |
| 一加 | OnePlus | 电池优化 | ✅ |
| 魅族 | MEIZU | 待机耗电管理 | ✅ |
| 三星 | SAMSUNG | 电池优化 | ✅ |
| 华硕 | ASUS | 自启动管理 | ✅ |

---

## 🎨 集成建议：在首次启动定位时引导

### 推荐实现

在用户**首次点击"开始定位"**时，弹出白名单引导：

```dart
Future<void> startLocation() async {
  // 1. 检查是否需要白名单引导
  bool needsGuidance = await _channel.invokeMethod('needsWhitelistGuidance');
  
  if (needsGuidance) {
    // 2. 检查是否已经提示过（避免每次都弹）
    SharedPreferences prefs = await SharedPreferences.getInstance();
    bool hasShownGuidance = prefs.getBool('whitelist_guidance_shown') ?? false;
    
    if (!hasShownGuidance) {
      // 3. 显示引导对话框
      await _showWhitelistGuidanceDialog();
      await prefs.setBool('whitelist_guidance_shown', true);
    }
  }
  
  // 4. 启动定位
  await _actuallyStartLocation();
}
```

---

## 🐛 常见问题排查

### ❓ 问题 1: 杀掉 APP 后服务没有重启

**排查步骤**:
1. 检查日志是否有 `START_STICKY` 相关信息
2. 检查是否添加到厂商白名单
3. 检查电池优化设置

**日志命令**:
```bash
adb logcat | grep "ForegroundLocationService"
```

**预期日志**:
```
D/ForegroundLocationService: onStartCommand called with action: START
D/ForegroundLocationService: Service will restart automatically (START_STICKY)
```

---

### ❓ 问题 2: 开机后服务没有自动启动

**排查步骤**:
1. 检查自启动权限
2. 查看开机广播是否收到

**日志命令**:
```bash
adb logcat | grep "BootCompletedReceiver"
```

**可能的日志**:
```
# 正常情况
D/BootCompletedReceiver: 收到系统广播: android.intent.action.BOOT_COMPLETED
D/BootCompletedReceiver: ✅ 前台定位服务启动成功（开机自启动）

# 异常情况
D/BootCompletedReceiver: 用户上次未启用定位服务，跳过自动启动
```

---

### ❓ 问题 3: 息屏后定位停止

**原因**: 
- 没有后台定位权限（`后台定位: denied`）
- WAKE_LOCK 没有生效

**解决**:
1. 引导用户开启 **"始终允许"** 定位权限
2. 检查 `WAKE_LOCK` 权限是否添加到 `AndroidManifest.xml`

---

### ❓ 问题 4: 华为手机无法后台定位

**华为特殊设置**:
1. **设置 → 应用和服务 → 应用启动管理**
   - 找到 Kissu
   - 关闭 "自动管理"
   - 手动开启：允许自启动、允许后台活动、允许关联启动

2. **设置 → 电池 → 应用启动管理**
   - 同样设置

3. **设置 → 位置服务 → Kissu**
   - 选择 **始终允许**

---

## 📊 测试报告模板

### 测试设备信息
- 设备型号: _______________
- Android 版本: _______________
- 厂商定制系统: _______________

### 测试结果

| 测试项 | 结果 | 备注 |
|--------|------|------|
| 手动杀掉后自动重启 | ☐ 通过 ☐ 失败 | ___ |
| 重启手机后自动启动 | ☐ 通过 ☐ 失败 | ___ |
| 息屏后继续定位 | ☐ 通过 ☐ 失败 | ___ |
| 白名单引导功能 | ☐ 通过 ☐ 失败 | ___ |

### 问题记录
```
[请在此记录测试中遇到的任何问题]
```

---

## 🚀 下一步优化建议

1. **在 APP 首页添加"后台运行状态"指示器**
   - 显示是否已添加到白名单
   - 显示定位权限状态（前台/后台/拒绝）

2. **定期检查服务状态**
   - 每 5 分钟检查一次服务是否存活
   - 如果服务被杀，尝试重启

3. **用户友好的引导流程**
   - 首次启动时的引导向导
   - 视频教程（针对不同厂商）

4. **统计数据上报**
   - 记录服务被杀的次数
   - 记录不同厂商的兼容性问题

---

## 📞 联系支持

如果测试过程中遇到问题，请提供：
1. 设备型号和系统版本
2. 完整的日志输出（`adb logcat`）
3. 测试步骤截图
4. 问题复现步骤

祝测试顺利！🎉



