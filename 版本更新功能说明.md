# 版本更新功能说明

## 功能概述
实现了应用版本检查和更新功能，支持三种更新模式：静默更新、弱更新和强更新。

## 实现文件清单

### 1. 新增文件
- `lib/network/public/version_api.dart` - 版本检查API
- `lib/services/version_service.dart` - 版本更新服务
- `lib/widgets/dialogs/version_update_dialog.dart` - 版本更新弹窗组件

### 2. 修改文件
- `lib/network/public/api_request.dart` - 添加版本检查API路径
- `lib/pages/mine/sub_pages/setting_about_us_page.dart` - 添加"检查更新"功能
- `lib/pages/home/home_controller.dart` - 添加首页自动检查更新
- `lib/main.dart` - 注册 VersionService

## API 接口

### 检查版本更新
**接口:** `/version/checkVersion`  
**方法:** GET  
**返回格式:**
```json
{
  "upgrade_type": 1,
  "version": "1.0.2",
  "version_num": 1000200,
  "content_txt": "<p>测试d</p>",
  "url": "http://com.cn"
}
```

### 参数说明
- `upgrade_type`: 更新类型
  - `1`: 静默更新（首页不弹窗）
  - `2`: 弱更新（可取消）
  - `3`: 强更新（必须更新，无取消按钮）
- `version`: 版本号字符串（如：1.0.2）
- `version_num`: 版本号数字格式（如：1000200，用于版本比较）
- `content_txt`: 更新内容（支持HTML格式，会自动去除标签显示）
- `url`: 下载地址

## 版本号转换规则
- 版本号格式：`major.minor.patch`（如：1.0.2）
- 数字格式：`major * 1000000 + minor * 1000 + patch`
- 示例：
  - 1.0.1 → 1000100
  - 1.0.2 → 1000200
  - 1.1.0 → 1001000

## 功能逻辑

### 1. 首页自动检查（`HomeController.onReady`）
- 延迟800ms后检查，避免影响首页加载
- 仅当有新版本且为强更新或弱更新时弹窗
- 静默更新不弹窗

### 2. 关于我们页面手动检查（`AboutUsPage`）
- 点击"检查更新"按钮触发
- 所有更新类型都会弹窗提示
- 已是最新版本时显示提示消息

### 3. 更新弹窗样式
- 背景图片：`assets/3.0/kissu3_update_dialog.webp`
- 强更新：只显示"立即更新"按钮
- 弱更新/静默更新：显示"稍后更新"和"立即更新"两个按钮

## 使用方式

### 在首页自动检查
```dart
// HomeController.onReady() 中已自动调用
_checkVersionUpdate();
```

### 在其他页面手动检查
```dart
final versionService = Get.find<VersionService>();
await versionService.checkVersionForAboutPage(context);
```

## UI 效果

### 弹窗布局
1. 顶部装饰区域（80px）
2. 标题："发现新版本"
3. 版本号：V1.0.2
4. 更新内容（可滚动，最高200px）
5. 按钮区域
   - 强更新：单个"立即更新"按钮
   - 弱更新/静默更新：左侧"稍后更新" + 右侧"立即更新"

### 按钮样式
- **立即更新按钮**
  - 渐变背景：#FF6B9D → #FF4081
  - 圆角：22px
  - 高度：44px
  - 文字颜色：白色

- **稍后更新按钮**
  - 白色背景
  - 灰色边框：#E6E2E3
  - 圆角：22px
  - 高度：44px
  - 文字颜色：#999999

## 注意事项

1. **版本比较逻辑**
   - 使用 `version_num` 数字格式进行比较
   - 只有新版本号大于当前版本号才会提示更新

2. **强更新行为**
   - 弹窗不可点击外部关闭（`barrierDismissible: false`）
   - 只有一个"立即更新"按钮
   - 点击按钮打开下载链接后，弹窗保持显示（强制用户更新）

3. **弱更新/静默更新行为**
   - 可以点击"稍后更新"关闭弹窗
   - 点击"立即更新"后打开下载链接并关闭弹窗

4. **下载链接处理**
   - 使用 `url_launcher` 在外部浏览器打开下载链接
   - 如果链接无法打开，会显示错误提示

## 测试要点

1. ✅ 首页启动时自动检查更新
2. ✅ 关于我们页面点击"检查更新"
3. ✅ 版本号正确比较（当前版本 vs 服务器版本）
4. ✅ 强更新弹窗：只有一个按钮，无法取消
5. ✅ 弱更新弹窗：两个按钮，可以取消
6. ✅ 静默更新：首页不弹窗，关于页面会弹窗
7. ✅ 点击"立即更新"正确打开下载链接
8. ✅ HTML 内容正确显示（去除标签和实体转换）

