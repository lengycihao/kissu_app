# 状态表情功能对接说明

## ✅ 已完成的工作

### 1. 📦 数据模型创建
**文件：** `lib/model/face_status_model.dart`

创建了以下数据模型：
- `FaceStatusResponseModel` - 接口响应根模型
- `FaceCategory` - 表情分类模型
- `FaceItem` - 表情项模型
- `CurrentFaceStatus` - 当前用户状态模型

### 2. 🌐 API 接口对接
**文件：** `lib/network/public/face_status_api.dart`

对接了 3 个接口：
- `GET /get/face` - 获取表情列表和当前状态
- `POST /save/face` - 设置用户状态（参数：face_id, face_expire）
- `POST /delete/face` - 删除用户状态

**文件：** `lib/network/public/api_request.dart`
- 添加了接口路径常量定义

### 3. 🎮 控制器逻辑实现
**文件：** `lib/pages/location/location_state_controller.dart`

实现的功能：
- ✅ 页面初始化时自动从接口加载表情数据
- ✅ 解析并显示当前用户状态（如果有）
- ✅ 选择表情并设置有效期
- ✅ 调用接口设置状态
- ✅ 调用接口删除状态
- ✅ 调用接口更新有效期
- ✅ 加载状态管理

### 4. 🎨 UI 界面更新
**文件：** `lib/pages/location/location_state_page.dart`

界面改进：
- ✅ 表情从文本 emoji 改为网络图片显示
- ✅ 添加加载中状态（CircularProgressIndicator）
- ✅ 错误处理（图片加载失败时显示占位图标）
- ✅ 顶部状态区域支持修改有效期并直接调用接口

## 📋 接口数据结构

### 获取表情列表响应（/get/face）
```json
{
  "face_list": [
    {
      "class_id": 1,
      "class_name": "心情想法",
      "face_list": [
        {
          "id": 1,
          "face_url": "https://...",
          "face_text": "亲亲"
        }
      ]
    }
  ],
  "now_face": {
    "id": 1,
    "face_text": "亲亲",
    "face_url": "https://...",
    "class_id": 1,
    "class_name": "心情想法",
    "face_expire": 1,
    "create_time": 1759865021
  }
}
```

**注意：** 
- `now_face` 为 `null` 或不存在时，表示当前没有设置状态
- `face_expire` 字段表示状态有效期（小时数）：1, 2, 4, 6, 8, 12

### 设置状态请求（/save/face）
```json
{
  "face_id": 1,
  "face_expire": 2
}
```

**参数说明：**
- `face_id`：表情ID
- `face_expire`：有效期（小时）：1, 2, 4, 6, 8, 12 分别对应页面中的 1小时、2小时、4小时、6小时、8小时、12小时

### 删除状态请求（/delete/face）
- 无需参数

## 🔄 业务流程

### 1. 进入页面
1. 自动调用 `/get/face` 获取数据
2. 解析 `face_list` 显示所有可选表情
3. 解析 `now_face` 显示当前状态（如果有）

### 2. 设置状态
1. 用户点击某个表情
2. 弹出底部选择有效期弹窗（1小时、2小时、4小时、6小时、8小时、12小时）
3. 选择有效期并点击确定
4. 如果已有状态，弹窗确认是否替换
5. 调用 `/save/face` 接口设置状态，传参 `face_expire` (1/2/4/6/8/12)
6. 更新本地状态显示

### 3. 删除状态
1. 点击顶部"删除"按钮
2. 弹窗确认
3. 调用 `/delete/face` 接口
4. 清空本地状态显示

### 4. 修改有效期
1. 在顶部状态区域选择新的有效期（1/2/4/6/8/12小时）
2. 直接调用 `/save/face` 接口更新，传参 `face_expire`
3. 更新本地显示
4. 从 `/get/face` 返回的 `now_face.face_expire` 会对应显示当前选中的有效期

## 🌐 环境配置

### 当前环境：测试环境
**文件：** `lib/network/tools/config/app_configN.dart`

```dart
static const serverEnvironmentTest = true;  // 测试环境
```

- **测试环境 API：** `http://dev-love-api.ikissu.cn`
- **生产环境 API：** `https://service-api.ikissu.cn`

### 切换环境
修改 `app_configN.dart` 第 6 行：
- 测试：`serverEnvironmentTest = true`
- 正式：`serverEnvironmentTest = false`

切换后需要**完全重启应用**（不是热重载）。

## 📝 关键代码说明

### 表情显示
使用网络图片：
```dart
Image.network(
  emoji.emoji,  // emoji.emoji 现在是图片 URL
  width: 46,
  height: 46,
  errorBuilder: (context, error, stackTrace) {
    return const Icon(Icons.image_not_supported, size: 46);
  },
)
```

### 状态有效期计算
```dart
// 当前状态的有效期
statusExpireTime.value = nowFace.createDateTime;

// 新设置状态的有效期
statusExpireTime.value = DateTime.now().add(Duration(hours: hours));
```

### 加载状态管理
```dart
// 加载中
isLoading.value = true;

// 显示加载指示器
if (controller.isLoading.value) {
  return const Center(child: CircularProgressIndicator());
}
```

## 🐛 调试信息

所有接口调用都有详细的日志输出：
- `📱` 获取数据成功
- `✅` 操作成功
- `⚠️` 操作失败
- `❌` 异常错误
- `ℹ️` 提示信息

可以在控制台查看详细的调试信息。

## 🎯 测试建议

1. **初始加载测试**
   - 打开页面查看是否正确加载表情列表
   - 检查当前状态是否正确显示

2. **设置状态测试**
   - 选择表情，设置不同有效期
   - 验证接口调用和状态显示

3. **替换状态测试**
   - 已有状态时选择新表情
   - 确认替换流程是否正常

4. **删除状态测试**
   - 删除当前状态
   - 验证UI恢复到无状态

5. **修改有效期测试**
   - 在顶部状态区域修改有效期
   - 验证立即生效

6. **网络异常测试**
   - 断网情况下的提示
   - 接口报错的处理

## 📌 注意事项

1. 表情图片使用网络加载，确保网络正常
2. 测试环境已配置为 `http://dev-love-api.ikissu.cn`
3. API 不加密（测试环境）
4. 切换环境后需要完全重启应用
5. 所有接口调用都有错误提示和加载状态

---

**创建时间：** 2025-01-07  
**最后更新：** 2025-01-07（添加 face_expire 字段支持）  
**对接接口：** `/get/face`, `/save/face`, `/delete/face`  
**测试环境：** http://dev-love-api.ikissu.cn

