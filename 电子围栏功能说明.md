# 🎯 电子围栏（位置提醒）功能说明

## 📋 功能概述

电子围栏功能允许用户设置特定位置的到达/离开提醒。当用户进入或离开设定的区域时，应用会自动发送通知提醒。

### 核心功能
- ✅ 在地图上选择位置并设置围栏
- ✅ 自定义围栏半径（100m - 1000m）
- ✅ 支持到达提醒和离开提醒
- ✅ 实时监测用户位置，判断进出围栏
- ✅ 触发时发送本地通知
- ✅ 在地图上可视化显示所有围栏
- ✅ 本地持久化存储
- ✅ 激活/暂停围栏开关

---

## 🏗️ 技术架构

### 1. 数据模型

#### LocationReminder（位置提醒模型）
```dart
class LocationReminder {
  final String id;              // 唯一标识
  final String name;            // 名称
  final String address;         // 地址
  final String icon;            // 图标类型
  final String note;            // 备注
  final double latitude;        // 纬度
  final double longitude;       // 经度
  final double radius;          // 围栏半径（米）
  final ReminderType type;      // 提醒类型（到达/离开）
  final bool isActive;          // 是否激活
  final Uint8List? mapSnapshot; // 地图快照
}
```

#### ReminderType（提醒类型枚举）
```dart
enum ReminderType {
  arrive,  // 到达提醒
  leave,   // 离开提醒
}
```

### 2. 服务架构

#### 📂 文件结构
```
lib/
├── pages/location/location_reminder/
│   ├── location_reminder_page.dart          # 位置提醒列表页面
│   ├── location_reminder_controller.dart    # 位置提醒控制器
│   ├── location_reminder_binding.dart       # 依赖注入绑定
│   ├── geofence_map_view_page.dart         # 围栏地图视图页面
│   └── location_picker/
│       ├── location_picker_page.dart        # 地图选点页面
│       └── location_picker_controller.dart  # 选点控制器
├── services/
│   └── geofence_monitoring_service.dart     # 围栏监测服务
└── plugins/amap_flutter_map-3.0.0/
    ├── lib/src/types/circle.dart           # Circle数据模型
    └── android/.../circle/                 # Android原生实现
```

#### 核心服务

**1. LocationReminderController**
- 管理位置提醒列表
- 本地存储（SharedPreferences）
- 围栏监测生命周期管理

**2. GeofenceMonitoringService**
- 实时监听位置更新
- 计算用户与围栏的距离
- 判断进入/离开状态
- 触发通知

**3. Circle覆盖物支持**
- Dart层：Circle数据模型
- Android原生：CirclesController
- 地图可视化显示

---

## 🚀 使用流程

### 用户操作流程

#### 1. 添加位置提醒
```
用户操作：
1. 进入"位置提醒"页面
2. 点击"添加地点"
3. 在地图上选择位置（点击地图或POI）
4. 选择图标类型（家/公司/健身房等）
5. 设置围栏半径（100m-1000m）
6. 选择提醒类型（到达/离开）
7. 输入备注（可选）
8. 点击"完成"保存
```

#### 2. 查看围栏地图
```
用户操作：
1. 在位置提醒列表页面，点击右上角"地图"按钮
2. 查看所有围栏的可视化展示
3. 蓝色圆圈 = 激活中的围栏
4. 灰色圆圈 = 已暂停的围栏
```

#### 3. 管理围栏
```
用户操作：
- 激活/暂停：点击围栏卡片上的开关按钮
- 查看地图：点击卡片上的地图图标
- 删除围栏：点击卡片上的删除按钮
```

---

## 🔧 技术实现细节

### 1. 围栏监测算法

#### Haversine距离计算
```dart
double _calculateDistance(double lat1, double lng1, double lat2, double lng2) {
  const earthRadius = 6371000.0; // 地球半径（米）
  
  final dLat = _toRadians(lat2 - lat1);
  final dLng = _toRadians(lng2 - lng1);
  
  final a = sin(dLat / 2) * sin(dLat / 2) +
      cos(_toRadians(lat1)) * cos(_toRadians(lat2)) *
      sin(dLng / 2) * sin(dLng / 2);
  
  final c = 2 * atan2(sqrt(a), sqrt(1 - a));
  
  return earthRadius * c;
}
```

#### 状态判断逻辑
```dart
// 计算距离
final distance = _calculateDistance(userLat, userLng, fenceLat, fenceLng);

// 判断是否在围栏内
final isInside = distance <= fenceRadius;

// 状态改变才触发
if (isInside != wasInside) {
  if (type == ReminderType.arrive && isInside) {
    // 触发到达提醒
  } else if (type == ReminderType.leave && !isInside) {
    // 触发离开提醒
  }
}
```

### 2. 通知机制

使用 `flutter_local_notifications` 插件发送本地通知：

```dart
await _notificationsPlugin.show(
  reminderId.hashCode,
  '📍 到达提醒',
  '你已到达 我的家（距离: 150米）',
  NotificationDetails(...),
);
```

### 3. 数据持久化

使用 SharedPreferences 存储围栏数据：

```dart
// 保存
final jsonList = reminders.map((r) => r.toJson()).toList();
final jsonString = json.encode(jsonList);
await prefs.setString('location_reminders', jsonString);

// 加载
final jsonString = prefs.getString('location_reminders');
final List<dynamic> jsonList = json.decode(jsonString);
final reminders = jsonList.map((json) => 
    LocationReminder.fromJson(json)).toList();
```

---

## 📊 性能优化

### 1. 位置更新频率
- 复用 `SimpleLocationService` 的定位数据
- 无需额外的定位请求
- 降低电量消耗

### 2. 监测策略
- 只监测激活状态的围栏（`isActive = true`）
- 状态改变时才触发通知（避免重复）
- 使用高效的 Haversine 算法计算距离

### 3. 内存管理
- 地图快照压缩存储
- 围栏数量建议不超过 20 个

---

## 🎨 UI/UX 设计

### 1. 位置提醒列表
```
┌─────────────────────────────────┐
│  位置提醒            [地图]     │
├─────────────────────────────────┤
│ ┌──────────────────────────────┐│
│ │ 🏠 我的家            [删除] ││
│ │ ┌────────────────────────┐ ││
│ │ │   地图快照显示区域     │ ││
│ │ └────────────────────────┘ ││
│ │ 浙江省杭州市上城区...        ││
│ │ [到达提醒·200米] [✓已激活][📍]││
│ └──────────────────────────────┘│
└─────────────────────────────────┘
```

### 2. 地图选点页面
```
┌─────────────────────────────────┐
│ [<]                    [完成]   │
├─────────────────────────────────┤
│                                 │
│      地图显示区域                │
│      + 圆形覆盖物显示            │
│                                 │
├─────────────────────────────────┤
│ 图标: [🏠][🏢][💪][🎮][🛒]      │
│ 围栏半径: 200米                 │
│ [100m][200m][300m][500m][1000m]│
│ 备注: [____________]            │
│ ○ 到达提醒  ● 离开提醒          │
└─────────────────────────────────┘
```

### 3. 围栏地图视图
```
┌─────────────────────────────────┐
│ [<] 围栏地图           [📍]    │
├─────────────────────────────────┤
│                                 │
│    多个围栏圆形显示              │
│    + 标记点                     │
│                                 │
│                                 │
│ ┌─────────────┐                │
│ │ 🔵 激活中   │                │
│ │ ⚪ 已暂停   │                │
│ │ 共3个围栏   │                │
│ └─────────────┘                │
└─────────────────────────────────┘
```

---

## 🧪 测试指南

### 1. 功能测试

**测试场景1：创建围栏**
```
步骤：
1. 进入位置提醒页面
2. 点击"添加地点"
3. 在地图上选择一个位置
4. 设置围栏半径为200米
5. 选择"到达提醒"
6. 保存

预期结果：
- 围栏成功保存
- 在列表中显示
- 地图上显示蓝色圆形
```

**测试场景2：触发提醒**
```
步骤：
1. 创建一个围栏（围栏半径200米，到达提醒）
2. 移动到围栏外（距离>200米）
3. 等待定位更新
4. 移动进入围栏内（距离<200米）

预期结果：
- 收到通知："你已到达 XXX"
- 通知显示距离信息
```

**测试场景3：激活/暂停**
```
步骤：
1. 创建一个围栏并激活
2. 点击"已激活"按钮切换为"已暂停"
3. 进入围栏范围

预期结果：
- 围栏显示为灰色
- 不触发通知
```

### 2. 边界条件测试

- 最小围栏半径：100米
- 最大围栏半径：1000米
- 多个围栏重叠时的表现
- 频繁进出围栏的通知防抖

---

## 📝 已知问题和限制

### 限制
1. **平台支持**：目前仅支持 Android，iOS 需要额外实现
2. **后台运行**：依赖定位服务持续运行
3. **精度限制**：GPS精度影响围栏判断准确性

### 优化建议
1. 建议围栏半径设置 ≥ 200米，避免GPS漂移误触发
2. 围栏数量建议控制在 20 个以内
3. 定期检查并删除不再使用的围栏

---

## 🔮 未来规划

### 短期计划
- [ ] iOS 平台支持
- [ ] 围栏历史记录
- [ ] 提醒统计（到达/离开次数）

### 长期计划
- [ ] 云端同步（多设备共享）
- [ ] 智能提醒（根据时间段自动启用/禁用）
- [ ] 轨迹回放
- [ ] 围栏分组管理

---

## 📞 技术支持

如有问题，请查看：
1. 日志输出（搜索关键字：`geofence`、`围栏`）
2. 检查定位服务是否正常运行
3. 检查通知权限是否已授权

---

## 📄 相关文档

- [Circle覆盖物使用说明](plugins/amap_flutter_map-3.0.0/CIRCLE_USAGE.md)
- [高德地图Flutter插件文档](https://lbs.amap.com/api/flutter/)

---

**更新日期**：2024-10-07  
**版本**：v1.0.0

