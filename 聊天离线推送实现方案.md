# 聊天消息离线推送实现方案

## 概述
当前项目使用腾讯云IM（TencentCloudChat）作为聊天SDK，需要集成腾讯云IM推送插件（TencentCloudChatPush）来实现聊天消息的离线推送功能。

## 需要完成的工作

### 1. 添加推送插件依赖

#### 1.1 检查依赖
首先需要确认 `tencent_cloud_chat_sdk` 是否已经包含了推送插件。如果没有，需要单独添加推送插件依赖。

在 `pubspec.yaml` 中添加（如果需要）：
```yaml
dependencies:
  # 腾讯IM推送插件（如果SDK不包含）
  tencent_cloud_chat_push: ^版本号
```

**注意**：根据腾讯云IM文档，推送插件可能需要单独引入，请查看最新的SDK文档确认。

### 2. 在IM登录成功后注册推送服务

#### 2.1 修改 `TencentIMService`
在 `lib/services/tencent_im_service.dart` 的 `loginIM` 方法中，登录成功后注册推送服务：

```dart
// 在 loginIM 方法中，登录成功后添加：
if (loginResult.code == 0) {
  _isLoggedIn = true;
  _currentUserID = user.uniqueId;
  
  // ... 现有代码 ...
  
  // 🔥 新增：注册推送服务
  await _registerPushService();
  
  return true;
}
```

#### 2.2 添加推送服务注册方法
在 `TencentIMService` 类中添加推送注册方法：

```dart
import 'package:tencent_cloud_chat_push/tencent_cloud_chat_push.dart';

/// 注册推送服务
Future<void> _registerPushService() async {
  try {
    // iOS需要配置APNS证书ID，Android可能需要FCM或使用厂商通道
    // 这些参数需要从腾讯云IM控制台获取
    final apnsCertificateID = Platform.isIOS ? 您的APNS证书ID : null;
    final appKey = "您的客户端密钥"; // 从IM控制台获取
    
    TencentCloudChatPush().registerPush(
      sdkAppId: sdkAppID,
      appKey: appKey,
      apnsCertificateID: apnsCertificateID,
      onNotificationClicked: ({required String ext, String? userID, String? groupID}) {
        // 处理推送通知点击事件
        _handlePushNotificationClick(ext: ext, userID: userID);
      },
    );
    
    logger.info('推送服务注册成功', tag: 'TencentIMService');
    
    // 可选：获取RegistrationID并上报给服务器
    _reportRegistrationID();
  } catch (e) {
    logger.error('推送服务注册失败: $e', tag: 'TencentIMService');
  }
}

/// 处理推送通知点击事件
void _handlePushNotificationClick({
  required String ext,
  String? userID,
}) {
  try {
    logger.info('推送通知被点击: ext=$ext, userID=$userID', tag: 'TencentIMService');
    
    // 解析ext字段，获取跳转信息
    // ext字段可能包含聊天相关的信息，如对方userID等
    
    // 跳转到聊天页面
    if (Get.currentRoute != KissuRoutePath.chat) {
      Get.toNamed(KissuRoutePath.chat);
    }
  } catch (e) {
    logger.error('处理推送通知点击失败: $e', tag: 'TencentIMService');
  }
}

/// 上报RegistrationID给服务器（可选）
Future<void> _reportRegistrationID() async {
  try {
    final registrationID = await TencentCloudChatPush().getRegistrationID();
    if (registrationID != null && registrationID.isNotEmpty) {
      logger.info('获取到推送RegistrationID: $registrationID', tag: 'TencentIMService');
      
      // TODO: 调用后端API上报RegistrationID
      // 示例：
      // await HttpManagerN.instance.executePost(
      //   ApiRequest.reportPushToken,
      //   jsonParam: {'registration_id': registrationID},
      // );
    }
  } catch (e) {
    logger.error('获取RegistrationID失败: $e', tag: 'TencentIMService');
  }
}
```

### 3. 配置推送证书和密钥

#### 3.1 iOS配置（APNS）
1. 在腾讯云IM控制台配置APNS证书
2. 获取证书ID（`apnsCertificateID`）
3. 在代码中使用该证书ID

#### 3.2 Android配置
Android平台支持多种推送通道：
- **FCM通道**：如果使用FCM，需要配置FCM相关参数
- **厂商通道**：小米、华为、OPPO、vivo等厂商推送通道
- **本机通道**：作为兜底方案

根据API文档，可以使用 `forceUseFCMPushChannel` 指定使用FCM通道。

### 4. 退出登录时反注册推送

在 `logoutIM` 方法中添加：

```dart
Future<bool> logoutIM() async {
  // ... 现有代码 ...
  
  // 🔥 新增：反注册推送服务
  try {
    TencentCloudChatPush().unRegisterPush();
    logger.info('推送服务已反注册', tag: 'TencentIMService');
  } catch (e) {
    logger.error('反注册推送服务失败: $e', tag: 'TencentIMService');
  }
  
  // ... 现有代码 ...
}
```

### 5. 添加推送监听器（可选）

如果需要监听推送消息的接收和撤回，可以添加推送监听器：

```dart
/// 添加推送监听器
void _setupPushListener() {
  final timPushListener = TIMPushListener(
    onRecvPushMessage: (TimPushMessage message) {
      logger.info('收到推送消息: ${message.toLogString()}', tag: 'TencentIMService');
    },
    onRevokePushMessage: (String messageId) {
      logger.info('推送消息被撤回: $messageId', tag: 'TencentIMService');
    },
    onNotificationClicked: (String ext) {
      logger.info('推送通知被点击: $ext', tag: 'TencentIMService');
      _handlePushNotificationClick(ext: ext);
    },
  );
  
  TencentCloudChatPush.addPushListener(listener: timPushListener);
}
```

### 6. 配置推送通知显示规则（可选）

如果不想在App前台时显示通知栏，可以调用：

```dart
// 关闭App在前台时弹出通知栏
TencentCloudChatPush.disablePostNotificationInForeground(disable: true);
```

### 7. Android原生配置（如果需要）

如果使用FCM或厂商通道，可能需要在Android原生代码中配置相关参数。当前项目已经有极光推送的配置，可以参考类似的配置方式。

## 配置参数说明

需要从腾讯云IM控制台获取以下参数：
1. **sdkAppId**：已在代码中使用（1600095370）
2. **appKey**：客户端密钥（需要从控制台获取）
3. **apnsCertificateID**：iOS APNS证书ID（仅iOS需要）

## 测试要点

1. **在线推送测试**：App在前台时，收到消息是否正常显示
2. **离线推送测试**：App在后台或完全关闭时，收到消息是否能收到推送通知
3. **点击跳转测试**：点击推送通知是否能正确跳转到聊天页面
4. **多设备测试**：同一账号在不同设备上登录，推送是否正常

## 注意事项

1. **隐私合规**：推送服务注册应该在用户同意隐私政策后进行
2. **权限申请**：Android需要申请通知权限
3. **证书配置**：iOS和Android的推送证书需要在腾讯云IM控制台正确配置
4. **RegistrationID管理**：卸载重装App会改变RegistrationID，需要重新上报
5. **推送去重**：避免同一消息重复推送

## 是否需要后端支持？

### ✅ **基本推送功能：不需要后端支持**

腾讯云IM的离线推送机制是**自动的**，不需要后端额外开发：

1. **自动推送机制**：
   - 当用户A发送消息给用户B时，如果用户B离线（App在后台或完全关闭）
   - 腾讯云IM服务器会自动检测到用户B离线
   - 自动通过配置的推送通道（APNS/FCM/厂商通道）发送推送通知
   - **整个过程完全由腾讯云IM SDK和服务器自动处理，无需后端介入**

2. **推送内容自动生成**：
   - 推送的标题、内容、声音等都由IM SDK自动处理
   - 推送中会携带消息的基本信息（发送者、消息类型等）
   - 通过 `ext` 字段可以携带自定义数据

### ⚠️ **可选的后端支持场景**

以下场景可能需要后端配合（**非必需**）：

1. **RegistrationID上报**（可选）：
   ```dart
   // 如果后端需要记录设备的推送token用于其他推送场景
   // 可以上报RegistrationID，但这不是聊天推送必需的
   await HttpManagerN.instance.executePost(
     '/report/push/token',  // 需要后端提供此接口
     jsonParam: {'registration_id': registrationID},
   );
   ```
   - **用途**：如果后端需要自己发送推送（非IM消息推送），需要知道设备的token
   - **是否必需**：❌ 不是聊天推送必需的，IM推送不依赖这个

2. **推送开关控制**（可选）：
   ```dart
   // 如果需要在后端控制是否推送（比如用户设置了免打扰）
   // 需要后端提供接口查询推送开关状态
   final result = await HttpManagerN.instance.executeGet('/get/push/settings');
   ```
   - **用途**：如果需要在后端控制推送开关
   - **是否必需**：❌ 不是必需的，可以在客户端本地控制

3. **自定义推送内容**（可选）：
   - 如果需要在推送中携带更多自定义数据，可以在发送消息时通过 `ext` 字段传递
   - 后端可以在发送消息时设置 `ext` 字段，推送时会自动携带

### 📋 **总结**

| 功能 | 是否需要后端 | 说明 |
|------|------------|------|
| **基本离线推送** | ❌ **不需要** | IM SDK自动处理 |
| **推送通知显示** | ❌ **不需要** | 系统自动显示 |
| **推送点击跳转** | ❌ **不需要** | 客户端处理 |
| RegistrationID上报 | ⚠️ **可选** | 仅用于其他推送场景 |
| 推送开关控制 | ⚠️ **可选** | 可在客户端实现 |
| 自定义推送内容 | ⚠️ **可选** | 通过消息ext字段传递 |

**结论**：实现聊天消息离线推送**不需要后端额外开发**，只需要：
1. ✅ 在腾讯云IM控制台配置推送证书和密钥
2. ✅ 在客户端注册推送服务
3. ✅ 处理推送通知点击事件

## 参考文档

- 腾讯云IM推送插件API文档：`api.md`
- 腾讯云IM官方文档：https://cloud.tencent.com/document/product/269

## 实施步骤建议

1. ✅ 确认推送插件依赖是否已包含在SDK中
2. ✅ 在腾讯云IM控制台配置推送证书和密钥
3. ✅ 在 `TencentIMService` 中添加推送注册代码
4. ✅ 实现推送通知点击跳转逻辑
5. ✅ 测试在线和离线推送功能
6. ✅ 可选：实现RegistrationID上报功能

